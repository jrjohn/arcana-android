services:
  android-build:
    build:
      context: .
      dockerfile: Dockerfile.ci
      platforms:
        - linux/amd64
    image: localhost:5000/arcana/android-app:${VERSION:-1.0.0}
    platform: linux/amd64
    volumes:
      - .:/project
      - gradle-cache:/root/.gradle
    command: ./gradlew assembleDebug --no-daemon

  android-test:
    build:
      context: .
      dockerfile: Dockerfile.ci
      platforms:
        - linux/amd64
    image: localhost:5000/arcana/android-app:${VERSION:-1.0.0}
    platform: linux/amd64
    volumes:
      - .:/project
      - gradle-cache:/root/.gradle
    command: ./gradlew testDebugUnitTest createDebugUnitTestCoverageReport --no-daemon --continue

volumes:
  gradle-cache:
